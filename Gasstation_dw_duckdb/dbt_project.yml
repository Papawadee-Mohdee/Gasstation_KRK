name: gasstation_dw
version: 1.0.0
config-version: 2
profile: gasstation_dw
model-paths:
- models
clean-targets:
- target
- dbt_packages
models:
  gasstation_dw:
    staging:
      +materialized: table
    datawarehouse:
      +materialized: table
